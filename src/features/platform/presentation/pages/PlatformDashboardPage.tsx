import { PlatformPulsePage } from "./PlatformPulsePage";

/**
 * Landing /platform — Pulso (ADR-0097). Única superficie; sin dashboard legacy.
 */
export function PlatformDashboardPage() {
  return <PlatformPulsePage />;
}
